services:
  backend:
    build: ./backend
    volumes:
      - ./data:/data
    env_file:
      - .env
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    volumes:
      - ./data:/data
    depends_on:
      - backend
    restart: unless-stopped
